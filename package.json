{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "In this article, we will create and host a fake server that we can deal with it as a normal Back-End Server and use all the CRUD Operations using HTTP requests.",
  "main": "server.js",
  "scripts": { 
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/8490/fake-server.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/8490/fake-server/issues"
  },
  "homepage": "https://github.com/8490/fake-server#readme",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
